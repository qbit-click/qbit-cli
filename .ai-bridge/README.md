# AI execution bridge

This directory is the transient ChatGPT/Codex execution channel. Runtime context, checkpoint requests, plans, diffs, logs, and generated status are non-canonical and should remain ignored unless a repository explicitly owns a durable bridge artifact.
